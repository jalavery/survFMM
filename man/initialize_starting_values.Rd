% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/initialize_starting_values.R
\name{initialize_starting_values}
\alias{initialize_starting_values}
\title{Initialize starting values for each initial partition of the EM algorithm
call}
\usage{
initialize_starting_values(
  n_inits,
  k,
  starting_values_type,
  starting_values_df = NULL,
  starting_values_window,
  input_df,
  outc_model_formula,
  weights_input,
  outc_distribution,
  starting_scale_logn = "exp"
)
}
\arguments{
\item{n_inits}{Number of initial partitions}

\item{k}{Number of subgroups}

\item{starting_values_type}{One of "single_survreg", "uniform_pct", or
"non_random_start". "single_survreg" fits a single AFT model to all of the
data and then generates random starting values based on the
`starting_values_window` parameter for each initial partition. If not
supplying starting values, they are randomly generated for each initial
partition. Be sure to set a seed at the top of your script to ensure
reproducibility.}

\item{starting_values_df}{Optional input dataset with starting values for algorithm}

\item{starting_values_window}{The percent margin around the starting values.
For example, starting_values_type = 'single_survreg' and
starting_values_window = 0.5 means that starting values are randomly
generated uniformly +/- 50% of the estimates from a single survreg model
fit. For starting_values_type = 'uniform' and starting_values_df is supplied, the starting values are generated uniformly}

\item{input_df}{Input data frame containing 1 row/observation, along with
each observation's event status (0=censored, 1=event) and event time
variables}

\item{outc_model_formula}{Formula object for subgroup-specific outcome models}

\item{weights_input}{Variable for inverse probability of treatment weights,
if applicable. For IPCW-FMM, supply the IPCW. For IPCW-FMM that also uses
IPTW, the product of the ITPW and IPCW may be supplied.}

\item{outc_distribution}{Outcome distribution for subgroup-specific outcome
models. Currently allowed values are "Weibull" and "Log-Normal" (not
case-sensitive)}

\item{starting_scale_logn}{One of "log" or "exp". Should
starting_values_window be on the log scale or unlogged scale (lognormal
distribution only). Unlogged scale is recommended so that results are
invariant to the outcome time scale.}
}
\value{
List of starting values, with 1 list element per set of starting values
}
\description{
Initialize starting values for each initial partition of the EM algorithm
call
}
